changed post

<% if activity.trackable %>
  on <%= link_to activity.trackable.content, activity.trackable%>
  <%=time_ago_in_words(activity.trackable.updated_at)+ " ago" %> 
  <% else %>
  that is no longer available
  <%end%>
