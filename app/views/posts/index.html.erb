<%= render 'posts/header' %>

<h1>All Posts</h1>
<br>

<table>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.user.name.capitalize if post.user %></td>
        <td><%= link_to post.content, post %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


