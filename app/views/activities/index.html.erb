<%= render 'posts/header' %>
<h1>Post Feed</h1>

<% @activities.each do |activity| %>
  <div class="activity">
    <%= link_to activity.owner.name.capitalize, activity.owner if activity.owner %>
    <%= render_activity activity %>  
  </div>
<% end %>

